# ======================================== #
# Class to do the variance decomposition
# ======================================== #

# =========================================================== #
# PREAMBULE
# Put in packages that we need
# =========================================================== #

import numpy as np

# =========================================================== #
# PREAMBULE
# Put in packages that we need
# =========================================================== #

class variancedecomp(object):

    # =========================================================== #
    # INITIALIZATION
    # =========================================================== #

    def __init__(self):
        print("# ==================================== #")
        print("# Initializing variancedecomp class    #")
        print("# ==================================== #")

    class variablelists(object):
        def read_variable_lists(self):
            3
    
    class metadata(object):
        def read_data(self):
            3
        
        def clean_data(self):
            3
    
        def transform_to_xarray(self):
            3
    
    class ipccdata(object):
        def read_data(self):
            3
        
        def clean_data(self):
            3
    
        def transform_to_xarray(self):
            3
    
    class decomposition(object):
        def drawing(self):
            3

    def save(self):
        3
